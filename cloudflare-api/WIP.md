# WIP — frozen scaffold

**Status:** non-shipping. Desktop product uses **WebDAV** sync (`src-tauri/src/sync.rs`), not this Workers API.

- Not part of `pnpm-workspace.yaml` or the Cargo workspace.
- Do not deploy for production Moon Translator.
- See `FEATURES.md` / stage audit T1-5 (Cloudflare scaffold hygiene).

Keep this folder only as a future optional cloud experiment; T1-5 desktop sync does not depend on it.
