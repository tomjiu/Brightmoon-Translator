# Dev archive (not built by Cargo / CI)

Stale vocabulary EventStore demos and integration sketches that no longer match
`moontranslator_lib` APIs. Kept for reference only.

CI uses `cargo check` + `cargo test --lib --bins` and does not compile these files.
